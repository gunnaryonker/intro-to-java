
public class JavaTest {
	public static void main(String args[]) {
		//Declare variables
		int number = 1;
		double quotient = number/2;
		while(quotient>=0.0001);
		{	
			(double) (quotient)= (double) (quotient)/2;
		}
		System.out.println(quotient);
	}
}

